version: '3'
services:
  my_container:
    image: my_image
    volumes:
      - /home/alonkay/Thesis/sequences:/home/alonkay/app/sequences
    # runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            # count: 1
            device_ids: ['0']
            capabilities: [gpu]
    stdin_open: true
    tty: true
    command: python3 Main.py
